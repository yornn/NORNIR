# Знаки полей

Сюда кладутся иконки инфоблока. Каждый файл — один знак, имя файла и есть его
ключ: панель просит знак по имени, CSS отдаёт файл (см. раздел `14. ICONS`
в `style.css`).

## Как рисовать

- SVG, `viewBox="0 0 24 24"`, квадрат.
- **Одноцветный силуэт.** Цвет внутри файла не важен вообще: знак подкладывается
  CSS-маской, а маска берёт только альфу. Пишите хоть `fill="#000"`.
- Никаких градиентов, `opacity`, встроенных `<style>`, растровых вставок.
- Обводки не тоньше `1.8` при 24px: знак рисуется в 13–16px.
- Поля внутри `viewBox` — около 1–2px, не больше: иначе знак усохнет рядом
  с текстом.
- Стиль единый: гравюра, линия под руны. Не глянцевые эмодзи.

## Как проверить

Положить файл в эту папку и перезагрузить расширение — больше ничего не нужно,
ни манифест, ни код не правятся.

**Файла нет — рисуется `_fallback.svg`** (ромб), и вёрстка этого не замечает.
Поэтому иконки можно доносить по одной, в любом порядке, а панель всё это время
остаётся рабочей.

---

## Что нарисовать

### Ярус 1 — обязательные

Сцена:

| Файл | Для чего |
|---|---|
| `_fallback.svg` | ✅ уже есть — заглушка для ненарисованных знаков |
| `scene-weather.svg` | погода |
| `scene-location.svg` | локация |

Время и календарь:

| Файл | Для чего |
|---|---|
| `time-eykt.svg` | эйкт, время суток |
| `time-sun.svg` | положение солнца. **Стрелку рисовать строго вверх** — восемь румбов панель докручивает поворотом, восьми файлов не нужно |
| `cal-weekday.svg` | день недели (Mánadagr…) |
| `cal-vika.svg` | номер недели |
| `cal-date.svg` | дата (7 Ýlir 998) |
| `cal-auknaetr.svg` | вставные дни Sumarauki |
| `season-sumar.svg` | лето (í Sumar) |
| `season-vetr.svg` | зима (í Vetr) |
| `moon-ny.svg` | новолуние |
| `moon-vaxandi.svg` | растущая луна |
| `moon-fullt.svg` | полнолуние |
| `moon-minnandi.svg` | убывающая луна |
| `moon-nid.svg` | безлуние |

Карточки {{char}} / {{user}}:

| Файл | Для чего |
|---|---|
| `char-attire.svg` | одежда |
| `char-state.svg` | состояние тела |
| `char-thought.svg` | невысказанная мысль |

Сводка об утробе:

| Файл | Для чего |
|---|---|
| `body-due.svg` | размер дитяти и срок («Дитя с кулак · ждать к исходу сольмануда») |
| `body-rest.svg` | «ещё можно попробовать отлежаться» — единственная надежда при угрозе |
| `body-father.svg` | «Отец: …» — признание, а не биология |
| `body-divination.svg` | «Толкуют: сын» — гадание, а не знание |

Дозор — восемь фактов, ради которых всё и затевалось:

| Файл | Для чего |
|---|---|
| `watch-kicks.svg` | шевеления дитяти («Дитя бьётся крепко» / «Дитя не слыхать 4 дня») |
| `watch-midwife.svg` | Льосмодир, повитуха |
| `watch-women.svg` | женщин в доме |
| `watch-charms.svg` | обереги |
| `watch-gear.svg` | наготове — вода, верёвка с балки |
| `watch-faderni.svg` | фадерни: признано / не признано / оспорено |
| `watch-rank.svg` | ранг дитяти: скирборинн, фриллуборинн, тюборинн, хорнунг |
| `watch-name.svg` | имя |

Отвар, дети, совет:

| Файл | Для чего |
|---|---|
| `draught.svg` | женское питьё (Einir, Malurt, Reinfáni, чёрные рожки) |
| `child.svg` | плашка дитяти |
| `child-need.svg` | нужда дитяти |
| `child-mark.svg` | веха: первый зуб, пошло само, первое слово |
| `advice.svg` | слово повитухи |

### Ярус 2 — фазы тела

Один знак отвечает за всё состояние героини: по нему видно, что с ней, без
чтения строк.

| Файл | Фаза |
|---|---|
| `phase-tidir.svg` | `Tíðir` — дни крови |
| `phase-hreinsun.svg` | `Hreinsun ok Vaxandi` — очищение и рост (цикл) |
| `phase-opit.svg` | `Opit ok Heitt` — открытое и горячее лоно |
| `phase-thverrandi.svg` | `Þverrandi` — тяжелеющее лоно |
| `phase-late.svg` | задержка без дитяти: голод, хворь, дорога, тревога |
| `phase-unknown.svg` | носит, но сама не знает |
| `phase-dulid.svg` | `Dulið` — сокрытое |
| `phase-kviknat.svg` | `Kviknat` — ожившее, дитя шевельнулось |
| `phase-falli.svg` | `Komin at falli` — подошедшая к падению |
| `phase-hridir.svg` | `Hríðir` — схватки |
| `phase-threat.svg` | угроза выкидыша или преждевременных родов |
| `phase-loss.svg` | выкидыш, мертворождение |
| `phase-saeng.svg` | `Liggja á sæng` — лежание после родов |
| `phase-hreinsan.svg` | `Hreinsan` — очищение после родов (**не** путать с `phase-hreinsun`: то фаза цикла, это послеродовое) |
| `phase-brjost.svg` | `Á brjósti` — дитя у груди |
| `phase-vending.svg` | `Vending` — тидир вот-вот вернутся |

### Ярус 3 — приметы

| Файл | Примета |
|---|---|
| `sign-breast.svg` | грудь набухла, соски потемнели; молозиво пришло |
| `sign-nausea.svg` | спит худо, мутит по утрам; не терпит запаха рыбы и дыма |
| `sign-false-hridir.svg` | утроба каменеет, да отпускает — ложные хридир |
| `sign-kicks-night.svg` | дитя пинает по ночам, спать не даёт |
| `sign-blood.svg` | кровь скудная, лицо бело как творог |
| `sign-lettari.svg` | `Léttari` — дитя опустилось, дышать легче, ходить тяжелее |
| `sign-strain.svg` | тягота: побили, упала, надорвалась, голодала — «утроба помнит» |
| `sign-odd-blood.svg` | кровь не в срок — дурной знак при дитяти |
| `sign-weather-toll.svg` | погода бьёт по телу: жара или мороз |
